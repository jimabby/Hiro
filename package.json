{
  "name": "hiro",
  "version": "1.0.0",
  "description": "Hiro — AI-powered automated job application suite (desktop app + mobile companion)",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "web": "npm --prefix web run dev",
    "web:build": "npm --prefix web run build",
    "app": "npm --prefix app start",
    "test": "npm --prefix web run test",
    "lint": "eslint .",
    "lint:web": "npm --prefix web run lint",
    "lint:all": "npm run lint:web && npm run lint"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "eslint": "^9.39.5",
    "eslint-plugin-react": "^7.37.5",
    "globals": "^17.8.0"
  }
}
